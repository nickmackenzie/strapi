'use strict';

/**
 * artist-spotlight controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::artist-spotlight.artist-spotlight');
